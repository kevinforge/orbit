# 更新日志

> English version: [CHANGELOG.md](./CHANGELOG.md).

---

## 未发布（Unreleased）

暂无变更。

---

## v1.3.0 — 2026-09-02

### 新功能

- **在会话中添加文件附件。** 可从输入框上传图片、PDF、文本、Markdown 以及
  常见源码/配置文件，并在消息历史中预览或下载。运行时支持原生图片能力时
  直接传递图片，其他文件通过本地 ACP resource link 交付。
- **使用各运行时的原生斜杠命令。** 在普通对话中输入 `/`，或在明确的员工
  指派标记后输入 `/`，即可发现、筛选、翻页并补全 ACP 公布的命令。
- **选择监工的运行时与模型。** 每个会话都能单独配置监工使用的运行时和
  模型，不影响数字员工团队配置。
- **访问 Orbit 产品官网。** 仓库新增独立产品落地页与 GitHub Pages 部署
  工作流。

### 改进与修复

- 隔离不同会话页面的上下文，避免上一页面的延迟事件或异步响应覆盖当前
  会话。
- 附件存储新增有界草稿、文件名恢复、安全下载、保留期清理和纯附件消息
  路由。
- CodeBuddy 的任务快照会显示在现有 Plan 面板中；原生命令发现支持冷启动、
  重试、大型命令列表，以及运行时公告与探测之间的竞态处理。
- 提及菜单与斜杠命令菜单完整支持输入法组合输入、Enter/Tab 补全、
  Shift+Enter 换行、Esc 关闭和键盘翻页。
- 运行时尚未结算时，流式最终答案不再在过程时间线与回复正文之间闪动。
- 更新官网构建依赖，包括 Vite、Wrangler 与 Cloudflare Vite 插件。

---

## v1.2.0 — 2026-08-21

### 新功能

- **看见每个回答背后的工作过程。** ACP 过程文本、原生 Plan 和按顺序排列的
  工具调用现在会展示在数字员工回复卡片中。
- **实时保留完整过程。** 数字员工工作期间，过程时间线会按顺序更新，工具组
  可以展开查看每一次调用。
- **完成后的界面更轻量。** 任务完成后执行过程默认折叠；刷新页面后恢复过程
  文本和工具调用统计，不持久化原始工具输入与结果。

### 改进与修复

- 三种 ACP runtime 均已覆盖：Claude Code、Codex 和 CodeBuddy。CodeBuddy 通过
  原生 agent phase 元数据识别多段模型响应边界。
- 原生 Plan 已从独立任务详情抽屉移动到数字员工回复卡片中。
- 运行结算后释放完整的内存活动记录，同时保留有界的短期取消结果索引。
- 新会话默认使用普通对话，输入框仍可切换简单协作和复杂协作。
- 新增 `find-simplifications` 和 `simplify-code` 项目 Skills。

---

## v1.1.0 — 2026-08-19

### 新功能

- **所有运行时统一走 ACP 协议。** Claude Code、Codex、CodeBuddy 全部通过
  Agent Client Protocol (ACP) v1 运行。Claude Code（`claude-agent-acp`）与
  Codex（`codex-acp`）适配器随 Orbit 内置，无需额外安装；CodeBuddy 通过
  `codebuddy --acp` 接入。
- **在会话内直接审批。** 数字员工的权限请求会以审批卡片出现在会话中，
  无需离开对话即可批准或拒绝。消息的审批模式（询问或完全访问）会沿整条
  交接链传递。
- **回答结构化提问。** 员工可以在会话中发起结构化提问（elicitation），
  工作过程中的原生计划（plan）也会同步展示。
- **按会话选择协作模式。** 普通对话、简单协作、复杂协作三种模式取代原先
  的固定路由行为。新会话默认使用简单协作，可在对话中途切换，员工会话
  上下文不会丢失。
- **交给内置监工协调。** 复杂协作模式启用内置监工：拆解目标、调度数字
  员工、跟踪进度、从失败中恢复并推动任务收尾。
- **自定义数字员工团队。** 员工完全可编辑：改名、改提示词、增删成员，或
  一键套用内置软件开发团队模板（范同经 / 甄架构 / 蔡一平 / 田小坑）。
  原先的逐员工权限设置已移除，统一由消息级审批模式控制。
- **一键停止所有任务。** 停止会话时，排队任务直接丢弃，运行中任务先进入
  cancelling 中间态等运行时结算，保留已产生的部分输出，不会混入下一个
  任务。

### 改进与修复

- **协作工作台全新改版。** 桌面风格的界面，新增任务详情抽屉、执行时间
  轴与更清晰的输入区（基于 tdesign-react）。
- **渲染更安全。** 员工输出中的外链仅允许打开 http(s) 地址，防范
  `javascript:` 与 `data:` 链接；ACP 过程性输出不再混入最终答复。
- **协调更可靠。** 监工运行期间停止按钮保持可见，监督运行中发送的用户
  消息会排队处理，交互超时计时器在 CI 环境下也能可靠触发。

---

## v1.0.0 — 2026-07-08

### 新功能

- **开源 1.0。** Orbit 1.0.0 以 MIT 协议公开发布，附带治理文件
  （`SECURITY.md`、`CODE_OF_CONDUCT.md`、`CONTRIBUTING.md`），并通过公开
  npm 与 GitHub Release 分发。
- **默认无需 license 文件。** 独立启动不再要求 `license.json`；私有授权
  构建可通过 `ORBIT_REQUIRE_LICENSE=true` 显式开启。

### 改进与修复

- **支持 npm 与发布包安装。** 包名发布为 `@kevinforge/orbit`，提供
  Windows x64、Linux x64、macOS x64/ARM64 独立二进制。
- **运行出错可恢复。** 处于错误状态的员工，在同一会话直接发送一条无
  指派消息即可重试。
- **上游繁忙提示更清晰。** Claude Code `529 overloaded` 失败会归纳为
  上游模型服务繁忙，不再重复原始报错。
- **本地数据有文档。** `docs/DATA_DIRECTORY.md` 说明 `~/.orbit` 的目录
  结构、备份、恢复与重置；`docs/TERMINOLOGY_AND_ROUTING.md` 定义产品
  术语与指派标记。

---

## v0.9.5 — 2026-06-21

### 新功能

- **看清工作如何完成。** 新增「协作洞察」页面，展示已完成与进行中的任务、参与数字员工、完成趋势、端到端耗时，以及能区分串行和并行执行的逐次运行时间轴。
- **跨会话继续工作。** 切换会话后数字员工仍可在后台执行，侧栏会标明每个会话中正在工作的数字员工。
- **交接关系更清楚。** 数字员工消息卡片会展示任务来源，便于追踪多员工之间的指派与交接链路。

### 改进与修复

- **协调更可靠。** 仅监督者参与的任务也会进入统计；监督者跟进时能保留触发消息的上下文；工作区模板新增依赖任务顺序指派规则，避免把前后依赖的工作错误并行。
- **任务结果更准确。** 中途取消一个排队节点，不再把后续已完成的整个任务误判为取消；最终取消、失败与恢复过程仍会保留在时间轴中。
- **队列不中断。** 取消正在运行的任务后，同一数字员工队列中的下一项会立即开始。
- **运行失败更可诊断。** CLI 异常退出时会保留有用的失败线索，不再只显示笼统的终端记录提示。
- **本地历史更安全。** 消息分片的恢复与保留逻辑能更稳妥地处理缺失或损坏文件，用户界面也不再泄漏内部运行术语。
- **安装指引更清楚。** 运行时安装命令与发布包安装说明已和 Orbit 页面中的实际提示保持一致。

---

## v0.9.4 — 2026-06-14

### 修复与稳定性

- **端口被占也能启动。** 当 Orbit 的端口（4317）已被占用——通常是因为上一次 Orbit 没有正常退出——现在会自动关闭那个残留进程并复用该端口，或换到下一个可用端口并告诉你端口号。不再卡在「端口被占用」上。
- **重启后不再有永远转圈的任务。** 上次关闭时仍在运行或排队的任务，现在会清楚标记为已中断（并提示重新发送），而不是一直显示「运行中」转个不停。
- **启动更稳。** 修正了高端口号下的一个端口识别边界问题，让上面的恢复逻辑更可靠。

---

## v0.9.3 — 2026-06-09

### 新功能

- **在输入框直接粘贴图片。** 把 PNG / JPEG / WebP 图片拖入或粘贴到输入框，发送前可预览，被指派的数字员工会连同文字一起收到图片。
- **一键复制数字员工。** 新增复制按钮，可把任意数字员工的配置克隆成一份默认关闭的新模板，方便做变体而不必重新填写。
- **真正停掉正在跑的任务。** 打断按钮现在会直接结束正在运行的进程（而不只是清队列），让失控的任务立刻停下、数字员工随即可以接手新工作。

### 日常体验更顺

- **直达数字员工设置。** 鼠标悬停在某个数字员工上，点齿轮图标即可直接打开它的配置。
- **缺工具时提示更清楚。** 当某个 CLI（Claude Code、Codex、CodeBuddy）未安装时，会显示醒目的提示条和安装按钮，不再是小链接。
- **支持 macOS 选目录。** 在 macOS 上也能像 Windows 一样选择工作区目录。
- **长内容更整齐。** 过长的代码块和链接不再撑破消息版面。
- **会话顺序不乱跳。** 点击某个会话不会把它顶到列表最上方。
- **界面中文更统一。** 整个应用（含数字员工设置页）统一为中文，"数字员工" 取代了旧的 "智能体" 措辞。

### 修复与稳定性

- **Codex 回答更干净。** Codex 的内部"旁白"不再混进你看到的答案；数字员工之间转交的消息也能在历史中正确保留。
- **打断反馈更安静。** 停止协作时不再弹出费解的内部提示，按钮直接显示 "已打断" 状态。
- **设置处处同步。** 修改某个数字员工的配置后，工作区内所有会话都会即时生效。
- **终端记录可关闭。** 新增工作区开关，不需要时可以关掉终端日志记录。
- **协调者更聪明。** 别的数字员工提到 `@user:` 时，不再误伤协调者；每个会话只允许一个协调者，内置的 PM 角色也被强化为真正的产品负责人。
- **附件更安全。** 上传的图片会按真实文件头校验（而非只看后缀），每个会话的草稿附件有数量上限，文件处理也不再卡住应用。

---

## v0.9.2 — 2026-06-04

### 新功能

- **迎来协调者角色。** 可以为工作区配置一个专门的协调（主管）数字员工：当消息没有指派给任何人、或某个数字员工卡住时，它会自动介入，让事情继续推进，省去你时时盯着的麻烦。
- **打断协作链，立刻说新事。** 当多个数字员工来回自动转交时，新的打断按钮会当场叫停后续转交链，你可以马上交代新任务；当前正在跑的任务仍会完成并展示结果。

### 改进

- **数字员工彼此更懂对方。** 交给每个数字员工的内部上下文被重组成清晰的分块结构，协作质量更高、误解更少。
- **历史不再无限膨胀。** 消息和终端记录按时间分片存储，并按时间与数量自动淘汰；旧消息通过"加载更早的消息"按钮按需加载，长会话也能保持轻快、有界。

---

## v0.9.1 — 2026-06-02

### 新功能

- **每个工作区一套指令与规则。** 可为每个工作区单独设置系统提示词和规则，数字员工自动套用，省去每次重复说明。
- **取消任务。** 新增取消按钮，可去掉某个数字员工队列里等待的任务（或停止正在运行的），队列会正确地接着跑下一个。

### 改进与修复

- **Codex 答案更稳。** 即使 Codex 的输出格式很刁钻，最终答案也能被稳定提取；紧凑版面也更清爽了。
- **权限设置不再丢。** 数字员工的权限配置现在能正确保存；当所需 CLI 未安装时，Orbit 会清楚标出该数字员工"不可用"。

---

## v0.9.0 — 2026-06-01

**Orbit 首个公开发布版本** —— 一个本地优先的协作控制台，在一个共享会话里
协调多个 CLI 后端的数字员工（Claude Code、Codex、CodeBuddy）。

- **一个频道，多个数字员工。** 用 `@名称:`（带冒号）来派活（冒号表示"去做这件事"），用不带冒号的 `@名称` 仅提及，或用 `@all:` 一次性派给所有人。
- **四个内置数字员工。** `@pm:`（产品）、`@architect:`（架构）、`@developer:`（开发）、`@tester:`（测试）开箱即用，各自权限不同——只有 developer 能写文件、提交代码、安装依赖。
- **自定义数字员工。** 可在界面里创建并配置自己的数字员工，包括权限。
- **工作区与会话。** 把工作划分到相互隔离的工作区和会话中，各自保留独立历史。
- **有序的任务队列。** 每个数字员工一次只跑一个任务，其余自动排队。
- **接着上次继续。** 会话跨次保留，数字员工可以续接之前的工作。
- **回答更易读。** 数字员工的回复以 Markdown 渲染，带语法高亮代码块，工具活动过程也实时可见。
